{application, proj,
 [{description, "A fault-tolerant tower defense game"},
  {vsn, "0.2.0"},
  {registered, [proj_app, main_server, db]},
  {mod, {proj_app, []}},
  {applications,
   [kernel,
    stdlib,
    gproc
   ]},
  {env,[]},
  {modules, [
    proj_app,
    main_supervisor,
    worker_supervisor,
    main_server,
    region_server,
    db,
    bloon,
    monkey,
    arrow,
    game,
    w
  ]},
  {licenses, ["Apache 2.0"]},
  {links, []}
 ]}.
